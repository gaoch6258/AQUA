# Python 3.11 recommended
# torch / vllm depend on CUDA
# trl is pinned to the dev build used during development
# local gs / radar_gs dependency is not included here

numpy==2.2.6
Pillow==12.1.0
h5py==3.15.1
datasets==4.5.0
torch==2.9.0
transformers==4.57.6
trl==0.28.0.dev0
peft==0.18.1
accelerate==1.12.0
vllm==0.12.0
tensorboard==2.20.0
